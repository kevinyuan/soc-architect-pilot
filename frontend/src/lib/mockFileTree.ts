// This file is no longer used as file system access is now server-side.
// It can be deleted or kept for reference.

// import type { FileSystemNode } from '@/types/ide';

// export const mockFileTree: FileSystemNode[] = [
// ];
